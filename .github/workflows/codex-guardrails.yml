name: codex-guardrails

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  guardrails:
    uses: ./.github/workflows/codex-guardrails-reusable.yml
    with:
      enforcement_mode: enforce
      project_type: mixed
